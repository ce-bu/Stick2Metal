# Override default variables for local builds

# Disable headless mode (show QEMU window)
headless = false

# Increase resources for faster build
memory = "4096"
cpus   = "4"

# Disk size (32GB is good for desktop with packages)
disk_size = "24G"

# Credentials
ssh_username = "ubuser"
ssh_password = "ubuser"
